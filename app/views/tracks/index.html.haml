%h1 Listing tracks

%table
  %tr
    %th Name
    %th Artist name
    %th
    %th
    %th

  - @tracks.each do |track|
    %tr
      %td= track.name
      %td= track.artist_name
      %td= link_to 'Show', track
      %td= link_to 'Edit', edit_track_path(track)
      %td= link_to 'Destroy', track, :confirm => 'Are you sure?', :method => :delete

%br

= link_to 'New Track', new_track_path
